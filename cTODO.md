# TODO

- Define concrete module structure under `src/` or `lib/`.
- Implement template repository persistence.
- Add RBAC policy enforcement layer with audit logging.
- Introduce encryption key rotation workflow.
- Add CI workflows under `.github/workflows/`.
